<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php 
    // OPERAÇÕES ARTMÉTICAS

    // abs(-200) -> mostra o valor absoluto (sem os sinais)

    // base_convert(205, 10, 16); -> transforma um número (decimal) em outro (hexadecimal)

    // ceil() -> arrendonda pra cima

    // floor() -> arredonda pra baixo

    // round() -> arrendonda padrão

    // hypot() -> hipotenusa

    // intdiv(5, 2) -> divisao inteira, quando nao se consegue mais dividir mostra o valor final, (5 / 2 = 2)

    // min() e max() -> mostra o maior e menor valor

    // pi() -> valor de pi
    // M_PI -> constante declarada com o valor de pi

    // pow(3, 2) -> potência ou 3 ** 2 nas versões novas, usar a função no lugar do operador vai perder a ordem de precedência

    // sin(), cos(), tan() -> seno, cosseno e tangente

    // sqrt(81) -> raiz quadrada 
    // raiz quadrada usando potência 81 ** (1/2)
    // raiz cúbica usando potência 27 ** (1/3)

    // OPERADORES ARITMÉTICOS

    // 5 + 2 = 7
    // 5 - 2 = 3
    // 5 * 2 = 10
    // 5 / 2 = 2.5 
    // 5 % 2 = 1 -> RESTO DA DIVISÃO
    // 5 ** 2 = 25 -> POTÊNCIA
    ?>
</body>
</html>